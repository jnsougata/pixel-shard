name: Deploy to Deta
on: push

jobs:
    deploy_0:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: BogDAAAMN/deta-deploy-action@v1.0.1
          with:
            deta-access-token: ${{ secrets.DETA_TOKEN }}
            deta-name: 'shard-zero'
            deta-project: 'default'

    deploy_1:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: BogDAAAMN/deta-deploy-action@v1.0.1
          with:
            deta-access-token: ${{ secrets.DETA_TOKEN }}
            deta-name: 'shard-one'
            deta-project: 'default'

    deploy_2:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: BogDAAAMN/deta-deploy-action@v1.0.1
          with:
            deta-access-token: ${{ secrets.DETA_TOKEN }}
            deta-name: 'shard-two'
            deta-project: 'default'

    deploy_3:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: BogDAAAMN/deta-deploy-action@v1.0.1
          with:
            deta-access-token: ${{ secrets.DETA_TOKEN }}
            deta-name: 'shard-three'
            deta-project: 'default'

    deploy_4:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: BogDAAAMN/deta-deploy-action@v1.0.1
          with:
            deta-access-token: ${{ secrets.DETA_TOKEN }}
            deta-name: 'shard-four'
            deta-project: 'default'

    deploy_5:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: BogDAAAMN/deta-deploy-action@v1.0.1
          with:
            deta-access-token: ${{ secrets.DETA_TOKEN }}
            deta-name: 'shard-five'
            deta-project: 'default'

    deploy_6:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: BogDAAAMN/deta-deploy-action@v1.0.1
          with:
            deta-access-token: ${{ secrets.DETA_TOKEN }}
            deta-name: 'shard-six'
            deta-project: 'default'

    deploy_7:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: BogDAAAMN/deta-deploy-action@v1.0.1
          with:
            deta-access-token: ${{ secrets.DETA_TOKEN }}
            deta-name: 'shard-seven'
            deta-project: 'default'

    deploy_8:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: BogDAAAMN/deta-deploy-action@v1.0.1
          with:
            deta-access-token: ${{ secrets.DETA_TOKEN }}
            deta-name: 'shard-eight'
            deta-project: 'default'

    deploy_9:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: BogDAAAMN/deta-deploy-action@v1.0.1
          with:
            deta-access-token: ${{ secrets.DETA_TOKEN }}
            deta-name: 'shard-nine'
            deta-project: 'default'